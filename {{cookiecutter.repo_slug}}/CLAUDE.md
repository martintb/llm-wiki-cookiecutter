Read `AGENTS.md` before making changes in this repository.

Canonical wiki skills live under `skills/` and are installed for Claude Code under `.claude/skills/`.

If Python helpers are present, refresh the harness skill mirrors with:

```text
python -m wiki_tools sync-skills
```
